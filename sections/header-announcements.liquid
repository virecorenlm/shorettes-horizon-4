

{%- liquid
  assign is_row = false
  if section.settings.announcement_layout == 'row'
    assign is_row = true
  endif
-%}

{% if section.blocks.size > 1 and is_row == false %}
  <script
    src="{{ 'announcement-bar.js' | asset_url }}"
    type="module"
    fetchpriority="low"
  ></script>
{% endif %}

{% render 'contrast-override', background_color: section.settings.background_color, section_id: section.id %}

<div class="section-background color-custom-{{ section.id }}"></div>
<aside
  class="announcement-bar spacing-style section section--{{ section.settings.section_width }} color-custom-{{ section.id }}"
  style="{% render 'spacing-padding', settings: section.settings %}; --border-bottom-width: {{ section.settings.divider_width }}px;{% if section.settings.divider_color != blank %} --color-border: {{ section.settings.divider_color }};{% endif %}"
>
  {% liquid
    assign autoplay = false
    assign show_arrows = false
    if section.blocks.size > 1 and is_row == false
      assign autoplay = true
      assign show_arrows = true
    endif
  %}

  {% capture contact_markup %}
    {% if section.settings.contact_text != blank %}
      {% if section.settings.contact_link != blank %}
        <a
          class="shorettes-announcement-contact"
          href="{{ section.settings.contact_link | escape }}"
        >
          {% render 'shorettes-icon', icon: 'phone', class: 'shorettes-announcement-contact__icon' %}
          <span>{{ section.settings.contact_text }}</span>
        </a>
      {% else %}
        <span class="shorettes-announcement-contact">
          {% render 'shorettes-icon', icon: 'phone', class: 'shorettes-announcement-contact__icon' %}
          <span>{{ section.settings.contact_text }}</span>
        </span>
      {% endif %}
    {% endif %}
  {% endcapture %}

  {% if is_row %}
    <div class="announcement-bar__row shorettes-announcement-row">
      <div class="announcement-bar__row-messages shorettes-announcement-row__messages">
        {% content_for 'blocks' %}
      </div>
      {{ contact_markup }}
    </div>
  {% else %}
    <announcement-bar-component
      class="announcement-bar__slider"
      {% if autoplay == true %}
        autoplay="{{ section.settings.speed }}" aria-live="polite"
      {% endif %}
    >
      {% if show_arrows %}
        {% render 'slideshow-arrows', icon_style: 'chevron' %}
      {% endif %}

      <div class="announcement-bar__slides">
        {% content_for 'blocks' %}
      </div>
    </announcement-bar-component>

    {{ contact_markup }}
  {% endif %}
</aside>

{% stylesheet %}
  .announcement-bar {
    border-block-end: var(--border-bottom-width) solid var(--color-border);
  }

  .announcement-bar__slider {
    display: flex;
    flex-direction: row;
    align-items: center;
    position: relative;

    @media screen and (max-width: 749px) {
      grid-column: 1 / -1;
    }
  }

  .announcement-bar__slides {
    display: grid;
    grid: [stack] auto / [stack] auto;
    width: calc(100% - var(--button-size) * 2);
    max-width: 680px;
    margin-inline: auto;
  }

  .announcement-bar__slides > * {
    grid-area: stack;
  }

  .announcement-bar__slide {
    transition: opacity 0.5s ease-in-out, visibility 0.5s ease-in-out;
    content-visibility: visible;

    &[aria-hidden='true'] {
      opacity: 0;
      visibility: hidden;
    }
  }

  .announcement-bar__slider slideshow-arrows {
    padding: 0;
    mix-blend-mode: normal;
  }

  .announcement-bar__slider slideshow-arrows .slideshow-control {
    color: var(--color-foreground);
  }

  .announcement-bar__slider .slideshow-control {
    display: flex;
    padding: 0;
    width: var(--button-size);
    height: var(--button-size);
    align-items: center;
    justify-content: center;
    opacity: 1;
    animation: none;

    @media screen and (min-width: 750px) {
      --slideshow-control-offset: calc((var(--button-size) - var(--icon-size-xs)) / 2);

      .section--page-width &.slideshow-control--previous {
        transform: translateX(var(--slideshow-control-offset));
      }
    }
  }

  .announcement-bar__slider .slideshow-control .svg-wrapper {
    width: var(--icon-size-xs);
    height: var(--icon-size-xs);
  }

  .announcement-bar__slide {
    place-content: center;
  }

  .announcement-bar__text:first-child {
    margin: 0;
  }

  .announcement-bar__link {
    position: absolute;
    inset: 0;
  }

  /* ---- Row layout ---- */

  .announcement-bar__row {
    grid-column: 1 / -1;
    width: 100%;
  }

  /* Announcements are laid out side by side, so the carousel's stacking and
     fade-out rules must not apply to them. */
  .announcement-bar__row-messages .announcement-bar__slide {
    position: static;
    opacity: 1;
    visibility: visible;
    transition: none;
  }

  /* Blocks default to `width: var(--width, 100%)`, which would stack them. */
  .announcement-bar__row-messages .announcement-bar__slide,
  .announcement-bar__row-messages .announcement-bar__text {
    width: auto;
  }

  /* A link inside a row announcement must not cover the whole strip. */
  .announcement-bar__row-messages .announcement-bar__slide {
    position: relative;
    flex: none;
  }

  /* Keep the contact detail on the same line as the slider when both are shown. */
  .announcement-bar:has(.shorettes-announcement-contact) {
    align-items: center;
  }

  @media screen and (max-width: 749px) {
    .announcement-bar__row-messages .announcement-bar__slide:nth-child(n + 3) {
      display: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.announcement_bar",
  "blocks": [
    {
      "type": "_announcement"
    }
  ],
  "enabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "select",
      "id": "announcement_layout",
      "label": "Layout",
      "info": "Row shows every announcement side by side on desktop. Rotating fades between them.",
      "options": [
        {
          "value": "carousel",
          "label": "Rotating"
        },
        {
          "value": "row",
          "label": "Row"
        }
      ],
      "default": "carousel"
    },
    {
      "type": "range",
      "id": "speed",
      "label": "t:settings.scroll_speed",
      "min": 2,
      "max": 10,
      "default": 5,
      "unit": "sec",
      "visible_if": "{{ section.settings.announcement_layout != 'row' }}"
    },
    {
      "type": "header",
      "content": "Contact"
    },
    {
      "type": "text",
      "id": "contact_text",
      "label": "Contact text",
      "info": "Shown at the end of the bar. Leave blank to hide."
    },
    {
      "type": "text",
      "id": "contact_link",
      "label": "Contact link",
      "info": "A full URL, or tel:+13156497387 to make the number tappable. Leave blank for plain text.",
      "visible_if": "{{ section.settings.contact_text != blank }}"
    },
    {
      "type": "header",
      "content": "t:content.appearance"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.section_width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "t:settings.background_color",
      "placeholder": "t:settings.default"
    },
    {
      "type": "range",
      "id": "divider_width",
      "label": "t:settings.divider_thickness",
      "min": 0,
      "max": 5,
      "step": 0.5,
      "unit": "px",
      "default": 0
    },
    {
      "type": "color",
      "id": "divider_color",
      "label": "t:settings.divider_color",
      "placeholder": "t:settings.default",
      "visible_if": "{{ section.settings.divider_width > 0 }}"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 15
    }
  ],
  "presets": [
    {
      "name": "t:names.announcement_bar",
      "blocks": {
        "announcement_1": {
          "type": "_announcement"
        }
      },
      "block_order": ["announcement_1"]
    }
  ]
}
{% endschema %}
